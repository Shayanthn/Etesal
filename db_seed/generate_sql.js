const { SAMPLE_CONFIGS } = require('../dist/data/configs.data.js') || { SAMPLE_CONFIGS: [] };
const { SAMPLE_PROXIES } = require('../dist/data/proxies.data.js') || { SAMPLE_PROXIES: [] };
const { KNOWLEDGE_ARTICLES } = require('../dist/data/articles.data.js') || { KNOWLEDGE_ARTICLES: [] };
const { SAMPLE_NEWS_ARTICLES } = require('../dist/data/news.data.js') || { SAMPLE_NEWS_ARTICLES: [] };

console.log("-- SQL SEED SCRIPT");
// generate inserts for configs
// generate inserts for proxies
// generate inserts for articles
// generate inserts for news
