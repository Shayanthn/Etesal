# 📋 سند جامع مشخصات و پیاده‌سازی فاز ۱ (Phase 1 Specifications & Implementation Plan)
## سیستم هویت با نام کاربری، پاپ‌آپ بازیابی ایمیل، سیستم کیف پول و دسترسی ادمین

این سند مشخصات فنی، معماری و چک‌لیست اجرایی فاز ۱ پروژه اتصال (Etesal Hub) است.

---

## ۱. منطق احراز هویت و ثبت‌نام (Username & Password Flow)

### ۱.۱. ثبت‌نام و ورود بدون نیاز به ایمیل اجباری:
- **ثبت‌نام (Sign Up):** فقط با **نام کاربری (Username)** و **رمز عبور (Password)** انجام می‌شود.
- **ورود (Sign In):** با همان نام کاربری و رمز عبور.
- **پاپ‌آپ راهنمای بازیابی ایمیل (Recovery Email Prompt Modal):** 
  - بلافاصله پس از ثبت‌نام موفق، یک پاپ‌آپ دوستانه به کاربر نمایش داده می‌شود:
  > *"برای اینکه اگه رمزت یادت رفت بعداً شرمندت نشیم، ایمیلت رو توی پنل وارد کن تا در صورت فراموشی بتونی حسابت رو برگردونی."*
  - کاربر می‌تواند در همان لحظه ایمیل خود را ذخیره کند یا دکمه «بعداً وارد می‌کنم» را بزند.

---

## ۲. سیستم کیف پول و مشترکین ویژه (Wallet & VIP Architecture)

### ۲.۱. تعریف مشترک ویژه (VIP):
- هر کاربری که در کیف پول خود موجودی داشته باشد یا اشتراک/کانفیگ اختصاصی فعال خریداری کرده باشد، به عنوان **کاربر ویژه (VIP)** شناخته می‌شود.
- **امکانات کیف پول:**
  - نمایش موجودی زنده به تومان (IRR / Toman).
  - افزایش موجودی (شارژ آنلاین / کارت به کارت / کریپتو / تتر).
  - خرید و تمدید آنی کانفیگ‌های اختصاصی با پینگ زیر ۳۵ میلی‌ثانیه و پورت ۴۴۳ اختصاصی.
  - ثبت ریزتراکنش‌ها (Transaction Ledger: شارژ، خرید، تمدید، هدیه ثبت‌نام).

### ۲.۲. طراحی ویژه اپلیکیشن اندروید (Mobile-First Experience):
- برای کاربران اپلیکیشن، تجربه کاربری روان، بدون فرم‌های پیچیده، با تمرکز مستقیم بر:
  - کارت موجودی کیف پول و دکمه شارژ سریع.
  - دکمه تمدید اشتراک با ۱ کلیک.
  - لیست کانفیگ‌های اختصاصی با دکمه اتصال و کپی آسان.

---

## ۳. امنیت پنل ادمین (Admin Route Protection)

- روت‌های `/admin` و `/master-admin` به وسیله کامپوننت `AdminRouteGuard` محافظت می‌شوند.
- کاربران عادی یا مهمان‌ها در صورت تلاش برای ورود به پنل ادمین با صفحه ورود امن و درخواست کلمه عبور/پین اختصاصی ادمین مواجه می‌شوند.

---

## ۴. چک‌لیست وضعیت پیاده‌سازی فاز ۱
 
- [x] **SEC-01:** ساخت `AdminRouteGuard.tsx` و امن‌سازی روت‌های `/admin` و `/master-admin`
- [x] **SEC-02:** بازنویسی `AuthModal.tsx` بر پایه نام کاربری + پسورد و اتصال به سیستم ورود/ثبت‌نام امن
- [x] **SEC-03:** ساخت مودال پاپ‌آپ ثبت ایمیل بازیابی پس از ثبت‌نام (`RecoveryEmailPromptModal.tsx`)
- [x] **SEC-04:** پیاده‌سازی سیستم و سرویس جامع کیف پول (`src/services/walletService.ts`)
- [x] **SEC-05:** طراحی و ادغام کامل بخش کیف پول و تراکنش‌ها در داشبورد وب‌سایت (`UserDashboard.tsx`)
- [x] **SEC-06:** بهینه‌سازی داشبورد اختصاصی اپلیکیشن موبایل با تمرکز بر کیف پول و تمدید اشتراک
- [x] **SEC-07:** افزودن هدرهای امنیتی و سخت‌سازی CSP در `index.html`
- [x] **SEC-08:** به‌روزرسانی گزارش وضعیت در `PRODUCTION_READINESS_AUDIT.md` و تایید نهایی
