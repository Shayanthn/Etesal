# سند جامع معماری و زیرساخت اتصال هاب (ETESAL Architecture Document)

این سند مرجع رسمی و نقشه راه معماری زیرساخت، جریان داده (Data Flow)، امنیت، اتوماسیون و پایگاه داده پروژه **اتصال (Etesal)** است.

---

## ۱. اصول حاکم بر معماری (Architectural Principles)

- **هزینه صفر ($0 Zero-Cost Infrastructure):** تمامی ابزارها و سرویس‌ها در لایه‌های رایگان معتبر و بدون نیاز به سرور ابری گران‌قیمت طراحی شده‌اند.
- **مقاومت در برابر فیلترینگ (Censorship Resilience):** استفاده از لبه شبکه (Edge)، CDN و پروکسی‌های توزیع‌شده برای دسترسی پایدار کاربران ایران.
- **امنیت بر پایه طراحی (Security by Design):** مخازن به صورت **کاملاً خصوصی (Private Repository)** نگهداری می‌شوند و هیچ کلید خصوصی، پسورد یا توکن حساسی در کلاینت یا سورس‌کد ذخیره نمی‌شود.
- **تفکیک لایه‌ها (Decoupled Layer Architecture):** منطق فرانت‌اند و استایل‌ها کاملاً از لایه پایگاه داده جدا هستند تا در مرحله نهایی با حداقل تغییرات به Supabase متصل شوند.

---

## ۲. پشته فناوری و زیرساخت (Technology Stack)

| لایه / بخش | ابزار انتخابی | نقش و مسئولیت | وضعیت هزینه |
| :--- | :--- | :--- | :--- |
| **لایه کلاینت و هاست** | **React 18 + Vite + Tailwind + Cloudflare Pages** | اجرای سریع رابط کاربری، روتینگ سئومحور، پاسخگویی به کاربران | رایگان ($0) |
| **لایه لبه و کش (Edge)** | **Cloudflare Workers + KV Cache** | توزیع جهانی، کش داده‌های لحظه‌ای، کاهش بار سروری | رایگان ($0) |
| **موتور هوش مصنوعی (AI & Translation)** | **OpenRouter (Free Models: DeepSeek, Llama 3, Gemini Flash Free)** | ترجمه تخصصی اخبار خارجی، خلاصه‌سازی، تولید تگ‌های سئو و اسلاگ انگلیسی | رایگان ($0) |
| **موتور اتوماسیون (Data Ingest)** | **n8n (Self-Hosted / Cloud Free Workflows)** | شنود کانال‌های تلگرام، دریافت فیدهای RSS اخبار، فیلتر و تمیزکاری کانفیگ‌ها | رایگان ($0) |
| **پایگاه داده و فایل‌ها (مرحله نهایی)** | **Supabase (PostgreSQL + Storage + Auth + RLS)** | احراز هویت کاربران، نگهداری کانفیگ‌های VIP و عمومی، ذخیره تیکت‌ها و آهنگ‌های ربات | رایگان ($0) |
| **اپلیکیشن اندروید (Mobile Client)** | **Capacitor + Sing-Box Core + Local/Codespaces Build** | اجرای نیتیو روی اندروید ۷ تا ۱۵، اتصال با پکیج `ir.aeherai.etesal` | رایگان ($0) |

---

## ۳. جریان جمع‌آوری و پالایش داده‌ها (Data Pipelines)

### الف) بخش اخبار فناوری، امنیت و شبکه
1. فیدهای RSS معتبر (دیجیاتو، زومیت، پیوست، BleepingComputer، Cloudflare Blog و...) توسط سناریوی n8n واکشی می‌شوند.
2. **ترجمه و سئو هوشمند با OpenRouter (مدل‌های رایگان):** اخبار انگلیسی از طریق API رایگان OpenRouter (مانند `deepseek/deepseek-chat:free`، `meta-llama/llama-3-8b-instruct:free` یا `google/gemini-flash-1.5:free`) ترجمه تخصصی، خلاصه‌سازی و تیتربندی سئومحور می‌شوند.
3. بر اساس دسته‌بندی موضوعی (فیلترینگ، لینوکس، امنیت، فیبر نوری، هوش مصنوعی) دسته‌بندی شده و تگ‌های سئو و اسلاگ انگلیسی تمیز دریافت می‌کنند.
4. در فرمت داده ساختاریافته (`NewsArticle Schema`) برای موتورهای جستجو آماده، کش و در دیتابیس ذخیره می‌شوند.

### ب) نودهای V2Ray / Reality / Hysteria 2
1. دریافت خودکار از کانال‌های تلگرامی و سابسکریپشن‌های معتبر توسط Worker / n8n.
2. تست پینگ و اعتبارسنجی سینتکس پروتکل.
3. تفکیک بر اساس اپراتورهای همراه اول، ایرانسل، رایتل و مخابرات و ارائه در سایت و اپلیکیشن.

### ج) پروکسی‌های MTProto تلگرام
1. استخراج سکرت‌های ضد فیلتر TLS و پورت‌های پرسرعت.
2. تست اتصال و ارائه دکمه‌های «اتصال با ۱ کلیک».

---

## ۴. استراتژی بیلد اپلیکیشن اندروید در مخزن Private

با توجه به **خصوصی (Private) بودن مخزن** و تکمیل دقایق رایگان GitHub Actions، از روش‌های زیر بدون نیاز به عمومی کردن کدها استفاده می‌شود:

1. **GitHub Codespaces (پیشنهادی):** استفاده از سهمیه ۶۰ ساعت ماهانه رایگان Codespaces جهت اجرای کامپایل مستقیم (`./gradlew assembleRelease`) در فضای ابری بدون اشغال سیستم کاربر.
2. **اسکریپت بیلد لوکال (Local Build Script):** اجرای اسکریپت کامپایل درون کلاینت توسعه‌دهنده با دستورات استاندارد Gradle و تولید سریع در پوشه `dist/`.
3. **GitLab Mirroring (اختیاری):** همگام‌سازی با مخزن Private در گیت‌لب جهت استفاده از ۴۰۰ دقیقه بیلد اختصاصی ماهیانه.

---

## ۵. نقشه راه پایگاه داده Supabase (فاز پایانی)

پایگاه داده در مرحله نهایی بدون تاثیر منفی روی فرانت‌اند متصل خواهد شد و شامل جداول زیر خواهد بود:
- `users`: مدیریت کاربران، نقش‌ها و سشن‌ها
- `configs`: آرشیو کانفیگ‌های عمومی و اختصاصی با برچسب پروتکل
- `proxies`: پروکسی‌های MTProto فعال با وضعیت پینگ
- `news`: مقالات و اخبار شبکه با اسلاگ اختصاصی سئو
- `tickets`: پیام‌های ارسال شده از فرم پشتیبانی و وضعیت پاسخگویی
- `media_storage`: محل نگهداری فایل‌های صوتی و مدیا برای ربات‌های تلگرام

---

## ۶. ورک‌فلوهای ماژولار n8n (Modular Workflows)

تمامی ورک‌فلوهای آماده n8n به تفکیک ماژول در مسیر `/workflows/n8n/` نگهداری می‌شوند و با کپی-پیست مستقیم در محیط کاربری n8n قابل استفاده هستند:
- **ماژول اخبار و سئو هوشمند:** `/workflows/n8n/news-ingestion-workflow.json` (شامل خوانش RSS، فیلتر کلمات کلیدی، اتصال به OpenRouter Free AI و ذخیره در Supabase)
- **راهنمای نصب و کلیدها:** `/workflows/n8n/README.md`

---

## ۷. داشبورد فرماندهی مدیریت کل (Master Admin Command Center)

پنل مدیریت در روت اختصاصی `/admin` با معماری ماژولار پیاده‌سازی شده و امکان رصد و کنترل یکپارچه کل سیستم را بدون نیاز به دسترسی مستقیم به سورس‌کد یا سرور فراهم می‌کند:
- **داشبورد و تله‌متری زنده (Live Telemetry & Health Matrix):** پایش بلادرنگ تعداد کاربران آنلاین، وضعیت سلامت لبه Cloudflare، درخواست‌های روزانه، پایداری (Uptime) و ماتریس سلامت سرویس‌ها.
- **مدیریت منابع ورودی (Sources & Feeds Ingest):** افزودن/حذف فیدهای RSS و کانال‌های تلگرامی، تغییر وضعیت فعال/غیرفعال آنی و تنظیم بازه واکشی خودکار.
- **تحریریه اخبار و سئو (News Editorial & AI Engine):** افزودن خبر با ترجمه خودکار هوش مصنوعی، تعیین فوری بودن اخبار (Breaking News) و حذف اخبار منسوخ.
- **مدیریت کانفیگ‌ها و Reality (V2Ray / Reality / Hysteria 2):** تست پینگ دسته‌جمعی نودها، افزودن کانفیگ دستی با تعیین اپراتورهای سازگار، کپی و اشتراک‌گذاری سریع.
- **پروکسی‌های MTProto تلگرام:** مانیتورینگ وضعیت پینگ، حذف و افزودن پروکسی‌های جدید با اتصال مستقیم به تلگرام.
- **هاب توزیع موزیک ربات (Music Bot & Audio Hub):** ثبت متادیتا، آپلود و ارسال مستقیم فایل‌های صوتی به کانال تلگرام به صورت بلادرنگ.
- **میز کار پشتیبانی و تیکت‌ها (User Support & Tickets Desk):** مشاهده تیکت‌های دریافتی، فیلتر بر اساس اپراتور (همراه اول، ایرانسل، شاتل و...)، استفاده از قالب‌های پاسخ آماده هوشمند و تغییر وضعیت به حل‌شده/در حال پیگیری.
- **لاگ‌های زنده سیستم (Real-time Audit Logs):** ثبت شفاف و تفکیک‌شده رویدادهای n8n، هوش مصنوعی OpenRouter، لبه CDN و عملیات‌های امنیتی مدیران.

---
*این سند با پیشرفت بخش‌های مختلف پروژه به‌صورت مستمر بروزرسانی خواهد شد.*
