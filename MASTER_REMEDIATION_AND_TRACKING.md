# 🛡️ Master Remediation & QA Tracking Matrix (Commercial-Grade)

این سند نسخه نهایی و تجمیع‌شده‌ی (Single Source of Truth) وضعیت پروژه است. تمامی مشکلات کدنویسی برطرف شده است و اکنون سیستم در فاز **تست و تایید نهایی در محیط پروداکشن (End-to-End QA)** قرار دارد.

هیچ‌یک از چک‌باکس‌های زیر بدون ارائه شواهد واقعی (Log، نتیجه کوئری یا اسکرین‌شات از سمت شما) تیک نخواهد خورد. لطفاً تست‌ها را فاز به فاز اجرا کرده و نتایج را به من گزارش دهید تا در صورت بروز خطا، دقیقاً همان بخش را دیباگ کنیم.

---

## ✅ فاز ۰: اصلاحات معماری سمت مخزن (انجام شده توسط AI)
* [x] **حذف آسیب‌پذیری Local Storage**: سیستم Auth به صورت 100% به Supabase متصل شد و لاجیک Fallback ناامن پاک شد.
* [x] **انتقال وب‌هوک تلگرام به Edge**: فایل‌های `src/server` پاک شدند و لاجیک بک‌اند به `workflows/cloudflare-worker/validator-worker.ts` منتقل شد.
* [x] **تامین امنیت پنل ادمین**: سیستم `AdminRouteGuard` برای اتصال مستقیم به Supabase Auth با ایمیل/پسورد بازنویسی شد.
* [x] **دیباگ کامل n8n**: محدودیت‌های ارسال پیام به تلگرام، یکتاسازی پروکسی‌ها (`O(1)` Set)، حل خطای JSON در کپشن‌ها و جایگزینی مدل‌های OpenRouter انجام شد.
* [x] **ایمن‌سازی کدهای Cloudflare Workers**: اصلاح آسیب‌پذیری‌های SSRF و Socket Leak در ورکر ولیدیتور، افزودن موازی‌سازی و robots.txt به ورکر سایت‌مپ، و ساخت فایل‌های `_redirects` و `_headers` برای SPA فرانت‌اند.

---

## 🗄️ فاز ۱: تست یکپارچگی پایگاه‌داده (Supabase SQL Tests) - [وضعیت: 🟢 تایید شد]
ممیزی و اسکن جامع پایگاه داده (X-Ray Audit) دریافت و نتایج کامل در پوشه `database/` ذخیره شد:

* [x] **تست 1.1: بررسی Constraint پروتکل کانفیگ‌ها**
  * **شواهد سیستم:** قید `configs_protocol_check` با پروتکل‌های `vless, vmess, hysteria2, trojan, tuic, shadowtls, ss` به درستی در لایه هسته دیتابیس فعال است.
* [x] **تست 1.2: بررسی Constraint پروکسی‌ها**
  * **شواهد سیستم:** ایندکس و قید یکتایی `unique_host_port` به همراه بازه پورت معتبر `1 <= port <= 65535` به صورت فعال روی جدول `proxies` برقرار است.
* [x] **تست 1.3: بررسی RLS (امنیت دسترسی)**
  * **شواهد سیستم:** قفل Row Level Security روی تمام ۹ جدول عمومی فعال است (`rls_enabled: true`). مستندات کامل در `database/AUDIT_SUMMARY_FA.md` ثبت شد.

---

## ☁️ فاز ۲: تست و اعتبارسنجی لبه (Cloudflare Verification) - [وضعیت: 🟡 در حال تست]
گزارش ممیزی کامل کدهای ورکر توسط Cloudflare AI در فایل `cloudflare-workers/AUDIT_MASTER_REPORT.md` ثبت شد.

* [ ] **تست 2.1: بیلد و لود فرانت‌اند در Cloudflare (SPA Refresh Check)**
  * **اقدام:** باز کردن مستقیم صفحه `https://etesal.aetherai.ir/admin` یا `https://etesal.aetherai.ir/help` با رفرش سخت (`Ctrl+F5`).
  * **انتظار:** صفحه بدون خطای 404 لود شود (به دلیل وجود `public/_redirects`).
* [ ] **تست 2.2: تست ماژول سنجش پینگ و سوکت TCP (`/validate`)**
  * **اقدام:** ارسال یک ریکوئست POST به `https://etesal.aetherai.ir/api/validate` با بدنه `{"host": "1.1.1.1", "port": 443}`.
  * **انتظار:** دریافت پاسخ JSON حاوی `valid: true`، مقدار `latencyMs` و کد لبه کلودفلر.
* [ ] **تست 2.3: بررسی درگاه Webhook تلگرام**
  * **اقدام:** ست کردن وب‌هوک با `setWebhook` در تلگرام به آدرس `https://etesal.aetherai.ir/api/telegram/webhook`.
  * **انتظار:** دریافت پاسخ `{"ok": true, "result": true}`.
* [ ] **تست 2.4: تست سایت‌مپ و robots.txt**
  * **اقدام:** باز کردن `https://etesal.aetherai.ir/robots.txt` و `https://etesal.aetherai.ir/sitemap.xml`.
  * **انتظار:** دریافت هدرهای کش مناسب و فرمت XML معتبر بدون ارور 500.

---

## ⚙️ فاز ۳: تست و گردش‌کارهای اتوماسیون (n8n Automation Engine) - [وضعیت: 🟢 اصلاح و آماده تست]
ممیزی خط‌به‌خط ۶ پایپ‌لاین در `workflows/n8n/AUDIT_MASTER_REPORT.md` و چک‌لیست در `workflows/n8n/ACTION_CHECKLIST.md` ثبت و تمام باگ‌های حیاتی و امنیتی در فایل‌های JSON برطرف شدند:

* [ ] **تست 3.1: پایپ‌لاین اسکرپ کانفیگ V2Ray (`1-config-ingestion.json`)**
  * **شواهد سیستم:** رفع قطعی سیم‌کشی نود ارسال به کانال تلگرام، افزودن User-Agent و لایه Retry و Timeout.
* [ ] **تست 3.2: پایپ‌لاین اسکرپ پروکسی MTProto (`2-proxy-ingestion.json`)**
  * **شواهد سیستم:** اضافه شدن هدر مرورگر کروم و تایم‌اوت ۱۵ ثانیه‌ای جهت جلوگیری از مسدودسازی توسط تلگرام.
* [ ] **تست 3.3: پایپ‌لاین ربات وایرال ادمین (`3-telegram-viral-bot.json`)**
  * **شواهد سیستم:** رفع باگ امنیتی `YOUR_ADMIN_ID`، اصلاح کوئری از جدول `proxies` و فرمت‌بندی HTML ضد کرش.
* [ ] **تست 3.4: موتور هوش مصنوعی اخبار ایران (`4-news-ingestion-iran.json`)**
  * **شواهد سیستم:** جایگزینی مدل‌های معتبر و رایگان Llama-3.3 و Gemini-2.0، افزودن `try/catch` و تصحیح انتساب منبع خبر بر اساس `$itemIndex`.
* [ ] **تست 3.5: موتور هوش مصنوعی اخبار بین‌الملل (`5-news-ingestion-global.json`)**
  * **شواهد سیستم:** رفع باگ ارجاع منبع خبر، هندلینگ خطا در پاسخ‌های JSON هوش مصنوعی و افزودن هدرهای Retry.
* [ ] **تست 3.6: سامانه متمرکز مانیتورینگ و لاگر خطا (`6-system-error-logger.json`)**
  * **شواهد سیستم:** تبدیل فرمت پیام از Markdown به HTML ضد خطا همراه با تابع ایمن‌سازی کاراکترها (`escapeHtml`).

---

## 📱 فاز ۴: تست وب‌سایت و اپلیکیشن
* [ ] **تست 4.1: سیستم احراز هویت (Auth)**
* [ ] **تست 4.2: پنل ادمین (Admin Guard)**

---
**وضعیت فعلی سیستم:** 🟡 فاز ۲ (تست و دیپلوی کلودفلر) آماده تست نهایی است.
